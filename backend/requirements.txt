Flask==3.1.3
flask-cors==6.0.2
Flask-JWT-Extended==4.7.1
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.49
bcrypt==5.0.0
PyJWT==2.12.1
joblib==1.5.3
numpy==2.4.4
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1
requests==2.33.1
gunicorn==23.0.0
psycopg2-binary==2.9.10
google-auth==2.35.0
cloudinary==1.41.0
flask-socketio==5.5.1
python-socketio==5.13.0
eventlet==0.40.0

# --- Geospatial engine (RUNTIME dependency for predictions) ---
shapely==2.1.2
# pyshp only needed for the one-time prepare scripts (not at runtime):
# pyshp
# --- Optional ML libs for LOCAL training only ---
# Deployed model is a scikit-learn Stacking ensemble (RF+GBR) -> needs only
# scikit-learn at runtime. Install these locally to train the full roster:
# xgboost
# lightgbm
# catboost
# shap

# --- AI chat assistant ---
openai==1.99.7

# --- rate limiting (brute-force protection) ---
Flask-Limiter==3.8.0
